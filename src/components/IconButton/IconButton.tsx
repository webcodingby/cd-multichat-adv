import styles from './IconButton.module.scss';
import { FC } from 'react'
import I from './types';

const IconButton:FC<I> = (props) => {
  const {
    variant
  } = props
  
  return (
    <button></button>
  )
}

export default IconButton;